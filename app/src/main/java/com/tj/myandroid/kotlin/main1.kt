package com.tj.myandroid.kotlin

fun main(array: Array<String>) {
    var person = Person(work = "mambar")
    println(person.name)
    println(person.age)
    println(person.work)
    println(person.sex)
    println(person.child)
}